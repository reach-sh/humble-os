# duoswap-core

This repository contains the source code for the HumbleSwap contracts.

# COMMANDS
+ `make clean` --- Clean
+ `make build` --- Build
+ `make check` --- Check files
+ `make test` --- Run tests
